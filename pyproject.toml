[project]
name = "responses"
version = "1.0.0"
description = "OpenAI Responses API Manifold"
requires-python = ">=3.11"
dependencies = [
    "aiohttp",
    "fastapi",
    "pydantic>=2",
]

[dependency-groups]
dev = [
    "black>=26.5.1",
]
